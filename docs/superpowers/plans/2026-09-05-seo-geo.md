# mechcrate.dev SEO + GEO Layer — Implementation Plan

**Spec:** `docs/superpowers/specs/2026-09-05-seo-geo-design.md`
**Branch:** `feat/site-seo`
**Verification toolkit:** cli (vitest + astro build + curl on preview) per task; playwright where noted.

Conventions: all work on `feat/site-seo`; commit per task in repo style; nothing outside `site/` except where a task says so (site.yml, docs comments); no `.env*`/node_modules/dist commits; bd handled by the orchestrator. Site app dir = `site/apps/site`. The bd pre-commit hook may sweep `.beads/issues.jsonl` into commits — amend it out (`--no-verify`) and restore prior staged state. Baseline before starting: `npm test` green, `npx astro build` green (~110 pages).

### Task 1: Edge files — robots.txt + _headers
- [x] Create `site/apps/site/public/robots.txt` exactly per spec §1 (User-agent *, Allow /, `Content-Signal: search=yes, ai-input=yes, ai-train=yes`, Sitemap absolute URL to `/sitemap-index.xml`).
- [x] Create `site/apps/site/public/_headers` per spec §1 (immutable `/_astro/*`; HSTS **without preload** + nosniff + referrer-policy on `/*`; text/plain charset + 1h cache on `/llms.txt` and `/llms-full.txt`; `X-Robots-Tag: noindex` on `/404`). Verify matcher syntax against wrangler 4.x Workers-assets `_headers` rules (read the installed wrangler bundle or docs — do NOT assume Pages syntax is identical).
- [x] Correct the false "_headers is a Pages mechanism" comments in `site/apps/site/wrangler.jsonc` and `.github/workflows/site.yml`.
- [x] Vitest: file-content assertions (signals line, sitemap line, headers rules present).
- [x] Local proof: `npx wrangler dev` (or workerd path used by CI's content-type check) → curl `/`, `/_astro/*` sample, `/llms.txt`, `/404` and assert the headers actually apply under Workers assets.
- **Accept:** build green; wrangler-served responses show immutable cache on an `/_astro/*` asset, HSTS on `/`, `text/plain; charset=utf-8` on `/llms.txt`, noindex header on `/404`; robots.txt served verbatim from the origin.

### Task 2: Freshness — explicit sitemap with real lastmod
- [x] Add `@astrojs/sitemap` as an explicit dependency + integration (verify Starlight 0.37.7 then skips its own injection — check its guard) with a `serialize` hook.
- [x] Build a source-path → ISO-date map at build time: corpus pages use `researched:` frontmatter when present; every other page (and corpus without it) uses the **git last-commit date of its source file** (`git log -1 --format=%cI -- <path>`, batched). Never build time. Expose the map to later tasks (module in `src/loaders/lib/dates.ts`).
- [x] `.github/workflows/site.yml`: add `fetch-depth: 0` to checkout in build AND deploy jobs (deploy re-checks out).
- [x] Vitest: frontmatter-beats-git; git date correct for a known committed file; every sitemap URL gets a lastmod.
- **Accept:** built `sitemap-index.xml`/`sitemap-0.xml` carries `<lastmod>` on 100% of URLs; a corpus doc with `researched: 2026-08-14` shows exactly that date; `npm test` green.

### Task 3: Social cards — per-page OG images
- [x] Add `astro-og-canvas@0.13.1`; build-time endpoint generating 1200×630 cards for the landing page and every docs/corpus page (title + section/category label, site dark palette + raccoon mark from existing assets; reuse `src/site-meta.ts`).
- [x] Inject via Starlight `head:` config for docs pages and direct tags in `src/pages/index.astro`: `og:image` (absolute), `og:image:width/height/alt`, `twitter:image`. `twitter:card` stays `summary_large_image`.
- [x] Vitest: every published page route has a corresponding card route (compare collections ↔ endpoint getStaticPaths).
- **Accept:** build green; `dist` contains one PNG per page; built HTML of `/`, a Start page, and a corpus page carries absolute og:image + dimensions; card PNG for the landing page opens as a valid 1200×630 image (verify bytes with `file`/imagemagick or a JS probe).

### Task 4: Structured data — JSON-LD
- [x] `src/components/Head.astro` Starlight override (register in astro.config `components`; keep `headDefaults` behavior intact by extending the default Head, not replacing its output): emits `BreadcrumbList` on all docs pages; plus `TechArticle` on corpus pages (headline, description, datePublished/dateModified from Task 2's date map, articleSection=category, `citation[]` from corpus.sources, `isBasedOn`=sourceUrl, publisher Organization).
- [x] Homepage (`index.astro`): `Organization` (logo, sameAs GitHub), `WebSite`, `SoftwareApplication` (mx: operatingSystem macOS/Linux, license URLs, free, codeRepository).
- [x] Vitest: ajv (or structural asserts) on generated JSON-LD objects per page type; dates match Task 2 map.
- **Accept:** built HTML: one valid `application/ld+json` block on `/` (3 entities), corpus page (TechArticle + BreadcrumbList), authored docs page (BreadcrumbList); JSON parses; `citation` count on `llm-token-cache-efficiency` page equals its sources count (24); `npm test` green.

### Task 5: Agent surface — llms.txt upgrades + markdown twins + section splits
- [x] llms.txt: add `## Instructions for LLM agents` near the top (honest contract per spec §5: install from source / no npm package, canonical commands, don't invent flags, where corpus + twins live, generated-at date **passed via build env or git date — never Date.now folklore**) and `## Optional` section demoting the 15 `Corpus: <category>` sections.
- [x] Markdown twins: prerendered endpoint serving each docs/corpus page's processed markdown at `<url>.md` — verify no route collision with the trailing-slash HTML routes; if Astro can't emit `foo/index.html` + `foo.md` side-by-side cleanly, fall back to `/md/<...slug>.md` and document the choice. Add `<link rel="alternate" type="text/markdown">` in the Head override (Task 4's component).
- [x] Section splits: generate `llms-guides.txt` + `llms-corpus-<category>.txt` alongside kept `llms-full.txt`; advertise all from llms.txt; extend `_headers` charset rules to cover `/llms-*.txt` and the twins (`*.md` responses text/markdown; verify matcher support, else use explicit paths).
- [x] Vitest: Instructions + Optional sections present with absolute URLs; twin content equals page markdown body for 3 sampled pages; each split file's docs sum to the full set; held-back docs absent everywhere.
- **Accept:** build green; `dist/llms.txt` has both new sections; `curl` of a twin serves markdown matching the page; split files sum = llms-full docs count; Pagefind index still excludes all txt/md artifacts; `npm test` green.

### Task 6: Polish — H1 dedup, head parity, icons, og:type, analytics beacon
- [x] Corpus H1 dedup in `src/loaders/lib/` (strip body's leading `#` when it fuzzy-matches frontmatter title; normalized-prefix comparison) + tests; verify sampled corpus pages have exactly one H1.
- [x] Landing head parity: `og:site_name`, `og:locale`, `twitter:title`, `twitter:description`, `<link rel="sitemap">`.
- [x] Icon set generated from the existing logo into `public/`: `favicon.ico` (16/32/48), `apple-touch-icon.png` (180×180), `site.webmanifest` (+ manifest link + `theme-color` meta on all pages via Head/landing).
- [x] `og:type=website` on section/category index pages (article only on content pages).
- [x] Cloudflare Web Analytics beacon on every page (Head override + landing): `<script type="module" src="https://static.cloudflareinsights.com/beacon.min.js" data-cf-beacon='{"token": "4c7a272fdcaf472ba395a69fbf05507c"}'></script>` — token as a constant in `src/site-meta.ts`; build must not break if the constant is emptied.
- **Accept:** Playwright on built site: exactly one H1 on 3 corpus pages; beacon script tag present on `/` and a docs page; icons return 200; landing has the 4 parity tags; a category index page shows `og:type=website`; zero console errors; `npm test` + build green.

### Task 7: Ship — full gates, PR, CI green
- [x] Full pass: `npm test`, `npm run build`, lychee offline link sweep, `mx rag ingest --dry-run` (0 warnings — nothing in docs/ should have changed), `make test` (Rust untouched).
- [x] Push `feat/site-seo`; open PR (base main) titled `feat(site): SEO + GEO layer — edge policy, social cards, freshness, schema, agent surface`; body summarizes per-spec sections + the owner checklist (Always Use HTTPS, managed robots toggle, Crawler Hints/IndexNow, Search Console DNS-TXT + sitemap submit, Bing import). Do NOT merge.
- [x] Watch `site.yml` build job to SUCCESS on the PR (preview deploy should also now succeed — Worker exists).
- **Accept:** PR open, build job green, preview job green (or its absence explained), body carries the checklist; post-merge live verification (merged robots.txt, http→301 after owner toggle, live headers, OG fetch) is listed in the PR as the follow-up.

## Out of scope
Everything in spec "Out of scope" (folklore list); Cloudflare dashboard actions (owner checklist); Starlight upgrade.
