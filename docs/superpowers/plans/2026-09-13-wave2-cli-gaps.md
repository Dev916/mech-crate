# Wave 2 — CLI Functionality Gaps (recipe correctness & unaided dev)

**Branch:** `fix/wave2-cli-gaps` (from main @ 25e06d9, post wave-1 merge)
**Tracking:** existing bd issues own tasks 1-5 (T1=lwe, T2=rqc, T3=47j+874+puy, T4=3kq, T5=4n4+xhf); T6 gets a new issue.
**Toolkit:** cli throughout. TDD discipline: red first where a test can express the defect; conformance nets extend the recipes suite; never weaken a test.

Conventions: all work on this branch; one commit per task in repo style; `make test` must stay green after every task (217 passed baseline post wave-1 — verify fresh at T1); no .env* commits; bd handled by the orchestrator; bd pre-commit hook sweeps .beads/issues.jsonl — commit with --no-verify and explicit pathspecs, restore staged state. Wave-1 invariant holds: every compose call in templates/scripts/*.sh carries `-p "$COMPOSE_PROJECT_NAME"` (templates_compose_isolation.rs enforces it) — new compose calls must too.

### Task 1: env_file precedence matches the documented order everywhere (bd mech-crate-lwe)
- [x] Documented order is .env.shared -> .env.secrets -> .env.<service> (last wins). 8 recipe compose files list secrets BEFORE shared (rust-api/rust-leptos/nuxt/astro service.yml; laravel service.yml + service.worker.yml + service.worker.dev.yml + service.scheduler.yml) and zola omits .env.secrets entirely. Reorder/complete all of them to the documented order.
- [x] Conformance net: extend the recipes suite so EVERY env_file list in templates/ and templates/recipes/ asserts the documented relative order (shared before secrets before service-specific) and that db-bearing recipes include .env.secrets. This is the regression net for the class, not just the 9 files.
- [x] Live E2E: scaffold one recipe in scratch, set a colliding key in .env.shared and .env.secrets, boot (or `docker compose config` if boot is genuinely too heavy — prefer the real boot) and prove the .env.secrets value wins inside the container env.
- **Accept:** all recipe env_file lists in documented order incl. zola carrying secrets; conformance test green and covering every compose file; collision key proven to resolve to secrets live; `make test` green.

### Task 2: db-bearing recipes `make dev` unaided — generated dev credentials (bd mech-crate-rqc)
- [x] Fresh `mx new` + `mx add <svc> --recipe rust-api` cannot `make dev` today: scripts/init.sh copies .env.secrets.template verbatim (empty DB_USER/DB_PASSWORD/DB_NAME) and rust-api ships `__GENERATE_DB_PASSWORD__` placeholders nothing replaces. Fix at the shared layer: init.sh (or the installer's post-install path) generates real dev credentials — the laravel recipe's generate-secrets.sh pattern is the in-repo precedent — so EVERY db-bearing recipe initializes unaided. Kill the `__GENERATE_DB_PASSWORD__` placeholder convention or make it the thing the generator replaces; no recipe may ship a placeholder that nothing consumes.
- [x] `make doctor` flags empty/placeholder values in .env.secrets cheaply (string check, no docker).
- [x] Tests: unit/template assertions on the generation path + conformance check that no recipe ships an unconsumed `__GENERATE_*__` placeholder.
- [x] Live E2E: fresh scratch project + rust-api recipe, ZERO hand edits, `make dev` reaches a healthy db and the app answers its health endpoint. Second recipe spot-check (laravel or nuxt) that init.sh produces non-empty secrets.
- **Accept:** rust-api boots unaided in scratch; generator covers all db-bearing recipes; doctor flags empties; `make test` green.

### Task 3: astro recipe repair — build target, health endpoint, dead payload, force_init (bd mech-crate-47j + 874 + puy)
- [x] 47j: astro service.yml `build.target: runner` names no stage in app.Dockerfile (stages: base/deps/builder/development/production) — every non-dev path fails. Align with the other 6 recipes (`target: production`). Also verify the production stage CMD makes sense for a scaffolded astro app (`node ./dist/server/entry.mjs` assumes server output); pick the least-wrong default and record it.
- [x] 874: recipe.json maps ONLY src/pages/api/health.ts, which imports `@/lib/db` + `@/lib/redis` under an alias create-astro's tsconfig never defines — /api/health 500s on a fresh scaffold. Design call (recorded): the scaffolder owns package.json and the app payload; the recipe owns infra + a dependency-free health endpoint (nuxt's is the model). Rewrite health.ts dependency-free; DELETE the unmapped dead payload files from templates/recipes/astro (they never land anywhere); recipes conformance asserts every recipe file is either mapped in recipe.json or infra (compose/dockerfiles/env templates).
- [x] puy: astro/nuxt/zola recipe.json declare `force_init` but no Rust reads it. Implement it in the installer (when set: remove the scaffold target before init_app so init_cmd re-runs — honor the documented semantics) with a unit test; wire nothing new into recipes beyond what they already declare.
- [x] Live E2E: fresh scratch + astro recipe: `docker compose config` resolves the production target; `make dev` boots and /api/health answers 200. force_init round-trip proven live with the astro scaffolder (or nuxt if faster): first add scaffolds, second add with force_init re-runs init_cmd.
- **Accept:** astro non-dev build path resolves; /api/health 200 on fresh scaffold; no unmapped payload files left; force_init implemented + tested; recipes conformance green for all 7; `make test` green.

### Task 4: multi-service selection — `make dev s="a b"` (bd mech-crate-3kq)
- [x] templates/make/dev.mk expands the s= value unquoted (second name becomes a make goal) and templates/scripts/.bashrc `compose_context_files` resolves exactly one docker/compose/${service}.yml. Fix both: quote the pass-through in the .mk layer and loop over a whitespace-separated service list in compose_context_files (each name contributes its compose file + dev override when present). Sweep the OTHER targets that take s= (up/stop/restart/logs/build at minimum): make each honor a list where the underlying script loops, or fail with a clear "single service only" message — no silent first-service-wins. Record which targets accept lists.
- [x] Compose project-name invariant: multi-service calls still carry `-p "$COMPOSE_PROJECT_NAME"` (the wave-1 test must stay green untouched).
- [x] Tests: template-level assertions on quoting + list handling (parse the generated commands with a docker stub if needed).
- [x] Live E2E: scratch project with TWO services (e.g. rust-api + astro), `make dev s="api site"` (real names) brings up exactly the selected pair and their deps; `make dev s=api` single-service path unchanged.
- **Accept:** multi-service selection proven live; non-dev s= targets either work with lists or refuse loudly; `make test` green.

### Task 5: compose hygiene — external network + container_name (bd mech-crate-4n4 + xhf)
- [x] 4n4: templates/recipes/rust-worker/docker/compose/service.yml joins `networks: internal: {external: true}` that nothing creates — `make dev` fails at runtime and `docker compose config` cannot catch it. Drop the external network (align with how the other recipes network) or have the project own it non-externally; record the choice. Add the check the config gate cannot do: conformance asserts no recipe/template declares an external network mx never creates.
- [x] xhf: drop hard-coded `container_name` from templates/docker/compose/{app,db,redis,nginx,worker,traefik}.yml and recipe fragments — with project names pinned (wave-1 71u) two stacks declaring `db` now fail with "name already in use". Compose's `<project>-<service>-1` naming takes over. Sweep the dependents: scripts/test-e2e.sh guard_container_names + docker rm teardown, make/ targets or scripts that exec by container name, router/traefik assumptions (routing is label-based — verify, don't assume). Conformance asserts no container_name remains in templates/ or recipes.
- [x] Live E2E: TWO scratch projects, each with a db-bearing recipe, `make dev` BOTH concurrently (unaided, thanks to T2): all containers up under compose-derived names, no name collisions, both apps answer through the router; `make down` on one leaves the other untouched.
- **Accept:** two same-shape stacks run concurrently with zero collisions; no container_name/phantom-external-network in shipped files; e2e script sweep done; `make test` green.

### Task 6: docs rider + ship
- [x] Docs where behavior changed: README + site pages (env precedence now true everywhere — restore any softened "always"; multi-service s= joins the subset-of-services pitch with real syntax; astro recipe notes; unaided `make dev` in start/first-project; container naming note for anyone who scripted against old fixed names — a breaking-change callout with the compose-derived name shape). KNOWN_BROKEN scoreboard consistent with any lane-test changes. llms.ts instructions block if it references any of these behaviors. Ride-along: the pending one-line RESEARCH_BACKLOG.md append (voice-agent pipelines topic, sitting uncommitted since 2026-08-26) — include it, flag it in the PR body.
- [x] Full gates: `make test`, `make check`; site: `npm test` + `npx astro build`; `mx rag ingest --dry-run` 0 warnings.
- [x] Push branch; PR base main titled `fix: Wave 2 CLI gaps — env precedence, unaided dev, astro recipe repair, multi-service dev, compose hygiene`; body: per-fix summary + breaking-change note (container names) + design calls recorded (astro payload ownership, network choice); watch site.yml + ci.yml to green. Do NOT merge.
- **Accept:** PR open, both workflows green, docs updated with the breaking-change callout, scoreboard consistent.
