---
title: Techniques Corpus
description: The published techniques corpus that ships inside mx's agent retrieval layer.
---

Coming soon.

This section will render every publishable document from the repository's
`docs/development/` corpus, auto-grouped by its frontmatter `category`
(architecture, concurrency, database, theory, patterns, and so on).

Each page will carry a banner noting that the doc ships inside mx's agent
corpus, where agents retrieve it via `rag_context`.
