#!/bin/bash
# Development mode for pro-docs with hot reload

set -e
cd /srv/app/pro-docs

export DOCS_PORT=${DOCS_PORT:-3081}
export RUST_LOG=${RUST_LOG:-pro_docs=debug,tower_http=debug}

exec cargo watch -x "run --package pro-docs --features ssr"
