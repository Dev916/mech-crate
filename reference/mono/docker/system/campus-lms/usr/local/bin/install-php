#!/bin/sh

set -e

apk add $(apk search -q php82-)
apk add php82-pear
apk del php82-pecl-psr

ln -s /usr/bin/php82 /usr/bin/php

ln -s /usr/sbin/php-fpm82 /usr/sbin/php-fpm

mkdir /run/php-fpm

chown -R www-data:www-data /var/log/php82
