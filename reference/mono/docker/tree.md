### .config - All config files used for every part of the application.

### compose - Modular compose files for every service / application.

### dockerfiles - All dockerfiles used for every service /application.

### system - All files used to source into docker build images. The should follow the exact file structure from the root of each services operating system.
