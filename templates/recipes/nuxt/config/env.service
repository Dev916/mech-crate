# {{SERVICE_NAME}} - Nuxt Application Configuration

# Development port (when not using Traefik)
{{SERVICE_UPPER}}_PORT=3000

# Public runtime config
NUXT_PUBLIC_API_BASE=/api

# Server-only secrets
API_SECRET=change-me-in-production
