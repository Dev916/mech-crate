# Shared Environment Configuration
# Generated by MechCrate Laravel Recipe
# Values shared across all services in this stack

# Database Connection (shared by app, worker, scheduler)
DB_USER={{SERVICE_SLUG}}
DB_NAME={{SERVICE_SLUG}}
DB_PASSWORD=${{{SERVICE_UPPER}}_DB_PASSWORD}

# Redis (shared)
REDIS_HOST=redis
REDIS_PORT=6379
