# MechCrate Global Router (Traefik)
# Workstation-wide reverse proxy for all MechCrate projects
#
# Start with: mx router up
# Dashboard:  http://localhost:${MX_ROUTER_DASHBOARD_PORT:-7680}

services:
  traefik:
    image: traefik:v3.6.1
    container_name: mx-router
    command:
      - --configfile=/etc/traefik/traefik.yml
    ports:
      # The router is a workstation singleton, so these stay pinned by default.
      # Overridable for a machine where something else already holds 80/443.
      - "${MX_ROUTER_HTTP_PORT:-80}:80"
      - "${MX_ROUTER_HTTPS_PORT:-443}:443"
      - "${MX_ROUTER_DASHBOARD_PORT:-7680}:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:rw
      - ./config/traefik.yml:/etc/traefik/traefik.yml:ro
      - ./config/dynamic:/etc/traefik/dynamic:ro
      - ./letsencrypt:/etc/traefik/acme
    networks:
      - devmesh-traefik
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "traefik", "healthcheck", "--ping"]
      interval: 10s
      timeout: 5s
      retries: 3

networks:
  devmesh-traefik:
    external: true
